# Nginx configuration for Laravel + Vue SPA
# This config is used in development mode

http {
    # Rate limiting zone
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

    # Include server configuration
    include /etc/nginx/conf.d/*.conf;
}
